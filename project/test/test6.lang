fun main() {
  sum int = 0;
  for(i, 1, 5, 1) {
    sum = sum + i;
  }
  writeInt(sum);
  writeln("");

  // now test nested while
  count int = readInt();
  while (count != 0) {
    writeInt(count);
    count = count - 1;
  }
  writeln("");
}